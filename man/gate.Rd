% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gate.R
\name{gate}
\alias{gate}
\title{gate() Function}
\usage{
gate(data, group = "id", gate = "type", size = 1, res = 50)
}
\arguments{
\item{data}{(Required) data.frame of nodes with coordinates. Must include columns for \code{x} and \code{y} coordinates, and at least one row where the gate type is \code{"and"}, \code{"or"}, or \code{"top"}.}

\item{group}{(Default = \code{"id"}) Character string naming the column in \code{data} that contains unique identifiers for grouping nodes. Default is \code{"id"}.}

\item{gate}{(Default = \code{"type"}) Character string naming the column in \code{data} that contains gate types. Must contain values \code{"and"}, \code{"or"}, or \code{"top"}. Default is \code{"type"}.}

\item{size}{(Default = 1) Numeric value controlling the diameter of gate polygons. Larger values create bigger gate shapes.}

\item{res}{(Default = 50) Numeric value controlling the number of line segments used to draw curved gate shapes. Higher values create smoother curves but require more computation.}
}
\value{
A data.frame (tibble) with columns \code{x} and \code{y} containing coordinate pairs that define gate polygon shapes. The data.frame is grouped by the \code{group} and \code{gate} columns, with one polygon per unique combination. Coordinates are centered at the node's position and scaled according to \code{size}.
}
\description{
This internal function gathers all gate polygons for a set of supplied gate types and their x and y coordinates.
}
\details{
This function generates polygon coordinates for visualizing gates in fault tree diagrams:
\itemize{
\item Filters nodes to only those with gate types (\code{"and"}, \code{"or"}, \code{"top"})
\item Groups nodes by their identifier and gate type
\item Calls \code{get_gate()} for each group to generate the appropriate polygon shape
\item Returns all polygon coordinates in a single data.frame suitable for use with \code{geom_polygon()} in ggplot2
}
The function is typically called internally by \code{illustrate()} but can be used directly to generate gate shapes for custom visualizations. Each gate type (AND, OR, top event) has a distinct shape generated by the corresponding \verb{gate_*()} function.
}
\seealso{
\code{\link{get_gate}} for generating individual gate polygons, \code{\link{gate_and}} for AND gate shapes, \code{\link{gate_or}} for OR gate shapes, \code{\link{gate_top}} for top event shapes
}
\keyword{fault}
\keyword{gate}
\keyword{maker}
\keyword{polygon}
\keyword{tree}
