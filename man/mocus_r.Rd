% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mocus_fast_r.R
\name{mocus_r}
\alias{mocus_r}
\title{mocus_r() — Fast Pure-R MOCUS Implementation}
\usage{
mocus_r(data)
}
\arguments{
\item{data}{data.frame produced by curate(). Must have columns:
gate (chr), type (chr/fct), class (chr/fct), items (list of chr vectors).}
}
\value{
Named list where each element is a character vector of basic events
forming one cutset. Identical structure to mocus() output.
}
\description{
Drop-in replacement for mocus() using a queue-based expansion, pre-indexed
gate lookup, and integer-encoded events. Same inputs/outputs as mocus().
}
\details{
Key improvements over original:
\enumerate{
\item Pre-indexes gate table into a named list — zero dplyr::filter() calls
inside the loop.
\item Integer-encodes every event/gate name once up front; all inner-loop
operations run on integer vectors (faster \%in\%, faster unique()).
\item Queue-based (append-only) expansion — never deletes/reallocates the
working list mid-loop. A tail pointer advances; new cutsets are
appended to the end.
\item Expands every gate in a cutset in a single pass rather than one gate
per outer-loop iteration.
\item Convergence is tracked with a simple counter, not by re-flattening
the entire list every iteration.
}
}
